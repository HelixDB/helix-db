[package]
name = "helix-cypher"
version = "0.1.0"
edition = "2024"

[dependencies]
helix-planner = { path = "../planner" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
